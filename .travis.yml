# Part of `travis-lazarus` (https://github.com/nielsAD/travis-lazarus)
# License: MIT

language: generic
sudo: required
dist: trusty

os:
  - linux
  - osx

env:
  global:
    - WINEPREFIX=~/.winelaz
    - DISPLAY=:99.0
  matrix:
    - LAZ_PKG=true  # Use the latest version from the default package manager
    - LAZ_VER=1.4.4 # Use specific (binary) release
    - LAZ_VER=1.8.4

install:
  # Install prerequisites (fpc/lazarus/wine/qemu)
  - if [[ $TRAVIS_OS_NAME == osx ]]; then
      brew install fpc caskroom/cask/brew-cask;
      brew cask install fpcsrc lazarus;
    else
      sudo apt -q install lazarus lcl-nogui;
    fi

script:
  - lazbuild ProjectEuler.lpi               # Build my_lazarus_test project
  - ./ProjectEuler # Run my_lazarus_test testsuite
  # - $LAZ_ENV ./bin/my_lazarus_tests --all --format=plain # Run my_lazarus_test testsuite

notifications:
  email:
    on_success: false
    on_failure: change